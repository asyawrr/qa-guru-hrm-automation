# Loaded as fallback when .env is missing. Optional .env overrides these values ( .env is gitignored ).
# Env 1 (default): demo — BASE_URL not set → demo URL, Admin/admin123.
# Env 2 (local/CI): set BASE_URL to localhost (or Docker: http://orangehrm:80) → asya_test/magqy8-...
BASE_URL=http://localhost:8080
API_BASE_URL=http://localhost:8080/web/index.php/api/v2
TEST_USER_USERNAME=asya_test
TEST_USER_PASSWORD=magqy8-namdux-witwoQ